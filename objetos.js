const sherlock = {
  surname: 'Holmes',
  age: 64,
};

console.log(sherlock.job?.boss);

// los bjetos se componnen de propiedades, estas propiedades funiconan como varibles,
// las propiedades se asignan
